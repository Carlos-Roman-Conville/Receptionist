# Production overrides — copy to repo root as .env before deploy

CLIENT_SLUG=crc-solutions
DEPLOYMENT_KIT_PATH=/deployment-kit

CHAT_ALLOWED_ORIGINS=https://crc-solutions.org
CHAT_ALLOW_NO_ORIGIN=0

VOICE_PUBLIC_WS_URL=wss://voice.crc-solutions.org
TELNYX_SKIP_SIGNATURE=0

ASYNC_WEBHOOK_SECRET=change-me

# Mount Deployment Kit read-only at /deployment-kit in compose override if needed
